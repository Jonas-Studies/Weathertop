mixin navbar (links)
	div(class="navbar navbar-expand border-bottom shadow")
		div(class="container-fluid")
			a(class="navbar-brand" href="/") WeatherTop
			ul(class="navbar-nav")
				each link in links
					li(class="nav-item")
						a(class="nav-link" href=link.url)= link.name
